name = "artifact-01-hello-world-markdown"
deploy = true

[harness]
type = "codex-cli"

[artifacts]
manifest = ".karta/artifacts.json"

[artifacts.kinds.hello-world-markdown]
renderer = "builtin.markdown"
schema_version = 1
portability = "static"
allowed_render_paths = ["output/**"]
allowed_evidence_paths = []
allowed_provenance_paths = []
allowed_continuation_paths = []
max_share_bytes = 10000
