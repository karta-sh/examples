name = "artifact-03-data-table"
deploy = true

[harness]
type = "codex-cli"

[artifacts]
manifest = ".karta/artifacts.json"

[artifacts.kinds.data-table]
renderer = "builtin.csv"
schema_version = 1
portability = "static"
allowed_render_paths = ["output/**"]
allowed_evidence_paths = []
allowed_provenance_paths = []
allowed_continuation_paths = []
max_share_bytes = 100000
